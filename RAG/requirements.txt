fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
boto3>=1.34.0
redis>=4.0.0
rq>=1.0.0

# Packages required by RAG.ingest and its full import chain
# framework_base.doc_store uses redis (already listed above)
langchain-core>=0.3.0
# framework_base.vector_store
chromadb>=0.3.0
langchain-chroma>=0.2.0
langchain-huggingface>=0.2.0
sentence-transformers>=2.0.0
# RAG.ingest direct imports
# Pin below 1.0 — langchain 1.x removed the `langchain.retrievers` namespace
# that MultiVectorRetriever lives in.
langchain>=0.3.0,<1.0.0
langchain-community>=0.3.0,<1.0.0
# Required by framework_base.llm_base, which pdf_loader imports for
# summary generation via the LiteLLM proxy (ChatOpenAI) and Ollama.
langchain-openai>=0.2.0,<1.0.0
langchain-ollama>=0.2.0,<1.0.0
# utils.py (root-level shared utility used by ingest.py)
tabula-py>=2.0.0
pandas>=1.5.0
PyMuPDF>=1.20.0
tqdm>=4.0.0
ipython>=8.0.0
# logger.py
structlog>=21.0.0
